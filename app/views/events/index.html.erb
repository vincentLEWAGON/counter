<H1>Event index</H1>


<% @events.each do |event| %>
<ul>
 <li>
 <%= link_to "#{event.name} #{event.start_date}", organization_event_path(current_user.organizations, event) %>
 </li>
</ul>
<% end %>

<%= link_to 'Create Event', new_organization_event_path(current_user.organizations) %>
