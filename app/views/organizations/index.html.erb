<h1>Mes organisations</h1>


<ul>
  <% @organizations.each do |organization| %>
    <li><%= link_to organization.name, organization_path(organization) %></li>
  <% end %>
</ul>


<%= link_to 'Créer une nouvelle organisation', new_organization_path %>
