

<h1>Organization Details</h1>

<p>Name: <%= @organization.name %></p>



<%= link_to 'Edit', edit_organization_path(@organization) %>
<%= link_to 'Voir les événements', organization_events_path(@organization) %>
<%= link_to 'Créer un nouvel événement', new_organization_event_path(current_user.organizations) %>

<%= button_to 'Delete', organization_path(@organization),
              method: :delete,
              data: { confirm: 'Are you sure you want to delete this organization?' } %>

<%= link_to 'Back', organizations_path %>
